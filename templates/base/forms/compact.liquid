{% block title %}{{ model.title }} : {{ global.site_title }}{% endblock %}
{% block content %}

<!-- INTRO SECTION - BEGINS -->
	<section class="col-24">
		<h1>{{ model.title }}</h1>
		<p class="mul-intro-text">
			The Compact Inline Form is intended to be used when there are a small number of fields used on the form and the form needs to be displayed in a tight area. It is normally used in conjunction with Placeholder Text, and usually without a corresponding Field Label.
		</p>
		<p><small>**Not all form layouts will work well together</small></p>
	</section>
<!-- INTRO SECTION - ENDS -->

<section class="col-24">

	<h3 class="mul-hdr-dbl-rule">
		Example
		<a href="javascript:;" class="mul-icon-size-small mul-icon-mul-arrow-circle-down mul-icon-color-gray-darker mul-icon-position-right mul-float-right related-items-menu"></a>
	</h3>

	<div class="col-24 example-min-height">

		{% include '/base/forms/form-compact' %}

	</div>

<!-- DETAILS SECTION - BEGINS -->
	<a name="moreDetails"></a>
	<h4 class="mul-hdr-dbl-rule additional-detail-hdr">Additional Details</h4>

	<div class="col-24">
		<div class="js-mul-tabs-example mul-tabs">
			{% include "page-tabs" %}
			<div id="tabs-1">
				<p>
					Use the Compact Inline Form when you only have a few fields on the Form. You can also use this form when you only have a small area in which to display the Form, like within a login container or an advanced search or filter functionality associated with a data grid.
				</p>
			</div>
			<div id="tabs-2">
				<h5>Labels</h5>
				<p>All field labels should be replaced with Placeholder text, which should use Title Case. See the <a href="/copy-text-styleguide">Copy/Text Style Guide narrative</a> for more detail.</p>
				
				<h5>Required Fields</h5>
				<p>Required fields are not denoted on a Compact Inline Form. </p>
				
				<h5>Error Handling</h5>
				<p>Should work as defined on the <a href="/base/forms/field-specific-error">Field Specific Error Message</a> page for details, with the following exception:</p>
				
				<ul>
				<li>There is no Required Icon, so the error version of the Required Icon should never be displayed.</li>
				</ul>
			</div>
			<div id="tabs-3">
				<h5>Placeholder Text</h5>
				<p>Some Form Fields contain Placeholder Text. However, IE9 doesn't understand the polyfill needed to create this type of text. In this case, you will need Java Script to put the Placeholder Text where it needs to go.</p>
			</div>
		<div id="tabs-4">
			<p>Examples go here&hellip;</p>
		</div>
		<div id="tabs-5">
		<!-- GET CODE - BEGINS -->
			<section class="col-24">

				<h5 class="mul-hdr-rule">HTML</h5>
				

				<pre class="prettyprint linenums"><xmp>{% include '/base/forms/form-compact' %}</xmp></pre>

			</section>
		<!-- GET CODE - ENDS -->
		</div>

	</div>
<!-- DETAILS SECTION - ENDS -->

</section>
{% endblock %}