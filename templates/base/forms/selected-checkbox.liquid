{% block title %}{{ model.title }} : {{ global.site_title }}{% endblock %}
{% block content %}

<!-- INTRO SECTION - BEGINS -->
	<section class="col-24">
		<h1>{{ model.title }}</h1>
		<p class="mul-intro-text">
			The Selected Checkbox is a form add-on that makes it easier for users to see the selections they have made on a form. When the user checks a selection, it will be highlighted in green.
		</p>
	</section>
<!-- INTRO SECTION - ENDS -->

<section class="col-24">

	<h3 class="mul-hdr-dbl-rule">
		Example
		<a href="javascript:;" class="mul-icon-size-small mul-icon-mul-arrow-circle-down mul-icon-color-gray-darker mul-icon-position-right mul-float-right related-items-menu"></a>
	</h3>

<!-- SELECTED CHECKBOX - EXAMPLE - BEGINS -->
	<div class="col-24 example-min-height">

		<div class="mul-box mul-box-rnd mul-ref-example-box">
			<h4 class="mul-hdr-rule">Stacked</h4>
			{% include '/base/forms/form-selected-checkbox' %}
		</div>

		<div class="mul-box mul-box-rnd mul-ref-example-box">
			<h4 class="mul-hdr-rule">Inline</h4>
			{% include '/base/forms/form-selected-checkbox-inline' %}
		</div>

	</div>
<!-- SELECTED CHECKBOX - EXAMPLE - ENDS -->

<!-- DETAILS SECTION - BEGINS -->
	<a name="moreDetails"></a>
	<h4 class="mul-hdr-dbl-rule additional-detail-hdr">Additional Details</h4>

	<div class="col-24">
		<div class="js-mul-tabs-example mul-tabs">
			{% include "page-tabs" %}
			<div id="tabs-1">
				<p>
					Use the Selected Checkbox when a form contains a large number of possible selections to a question, so users can see what exactly they have checked off. For example, if a user needs to check off countries visited or hobbies/interests, there will most likely be a lot of options on the form. The Selected Checkbox is the perfect option to allow users to keep track of their selections.
				</p>
			</div>
			<div id="tabs-2">
				<p>
					When a checkbox is initially selected the Active State (see below) is highlighted using the "Attention" color and the green background indicates selection.
				</p>

				<h5>Margins</h5>
				<p>
					In order to keep with Mercer consistency, the margins between Elements have been standardized:
				</p>
				<ul>
					<li>
						Use a 10 pixel bottom between the Element and next Field Label.
					</li>
					<li>Use a 3 pixel bottom margin between a Label and an Input.</li>
				</ul>

				<h5>Labels</h5>
				<p>
					All field labels should use Title Case. See the
					<a href="/copy-text-styleguide">Copy/Text Style Guide narrative</a>
					for more detail.
				</p>

				<h5>Active State</h5>
				<p>
					The active state for any field should be indicated by the application of the "Attention" color to the border of the specific field the user has clicked into. See
					<a href="/base/colors">Color Palette</a>
					for more details on "Attention" color.
					<br>						
					<Br>						
					Designers can apply the CSS class ("mul-checkbox-checked") to the
					<a href="/base/forms/checkbox">Checkbox</a>
					element in order to make it a Selected Checkbox with the green background.
				</p>

				<h5>Required Fields</h5>
				<p>
					All required fields should be denoted by placing the  Required icon (the "asterisk-required.png" image from the
					<a href="/base/icons">list of icons</a>
					) immediately to the left of the Field Label.
				</p>
				
				<h5>Error Handling</h5>
				<p>
					See the
					<a href="/base/forms/field-specific-error">Field Specific Error Message</a>
					page for details.
				</p>
			</div>
			<div id="tabs-3">
				<h4 class="mul-hdr-rule">Dependencies</h4>
				<table class="mul-table mul-table-borders" id="" border="0" cellpadding="0" cellspacing="0" title="" summary="">
					<thead>
						<tr>
							<th width="" scope="col">Library</th>
							<th width="" scope="col">Location or Download</th>
						</tr>
					</thead>
					<tbody>
						<tr>
							<td>jQuery {{ global.jquery_version }}</td>
							<td>assets/scripts/core/lib/jquery</td>
						</tr>
					</tbody>
				</table>
			</div>
			<div id="tabs-4">
				<p>{{ model.mobileIntroTabText }}</p>
			</div>
			<div id="tabs-5">
			<!-- GET CODE - BEGINS -->
				<section class="col-24">

					<h5 class="mul-hdr-rule">HTML</h5>
					<p>
						The selected checkbox allows the user to click and higlight a larger area. For this to work you would need to place 
						a class of <code>js-mul-selected-checkbox</code> on the <code>form</code> tag.
					</p>

					<h5>Stacked</h5>
					<pre class="prettyprint linenums"><xmp>{% include '/base/forms/form-selected-checkbox' %}</xmp></pre>

					<h5>Inline</h5>
					<pre class="prettyprint linenums"><xmp>{% include '/base/forms/form-selected-checkbox-inline' %}</xmp></pre>

				</section>
			<!-- GET CODE - ENDS -->
			</div>
		</div>	
	</div>
<!-- DETAILS SECTION - ENDS -->

</section>
{% endblock %}