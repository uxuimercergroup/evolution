{% block title %}{{ model.title }} : {{ global.site_title }}{% endblock %}
{% block content %}

<!-- INTRO SECTION - BEGINS -->
	<section class="col-24">
		<h1>{{ model.title }}</h1>
		<p class="mul-intro-text">The Product Header With Static Navigation is one of the default product headers offered in Evolution. This version contains a static gray main navigation bar that displays immediately below the blue product identifier background.</p>
	</section>
<!-- INTRO SECTION - ENDS -->

<section class="col-24">

	<h3 class="mul-hdr-dbl-rule">
		Example
		<a href="javascript:;" class="mul-icon-size-small mul-icon-mul-arrow-circle-down mul-icon-color-gray-darker mul-icon-position-right mul-float-right related-items-menu"></a>
	</h3>

<!-- PRODUCT HEADER WITH NAVIGATION - BEGINS -->
	<div class="col-24 example-min-height">
		
		<a class="mul-btn mul-btn-medium mul-btn-primary mul-btn-first" href="/demos/layout-default-demo" target="_blank">View Full Page Example</a>

	</div>
<!-- PRODUCT HEADER WITH NAVIGATION - ENDS -->

<!-- DETAILS SECTION - BEGINS -->
	<div class="col-24">
		<a name="moreDetails"></a>
		<h4 class="mul-hdr-dbl-rule addtional-detail-hdr">Additional Details</h4>
		

		<div class="js-mul-tabs-example mul-tabs">
			{% include "page-tabs" %}
			<div id="tabs-1">
				<p>This version of a product header should be used when it is important to provide a consistent main navigation option that is displayed on every screen, and when the number of main navigation options is manageble number (less than 7 or so).</p>
			</div>
			<div id="tabs-2">
				<p>This header is comprised of the following sections:</p>
				
				<h5>Product Identifier</h5>
				<ul>
					<li>This is the first bar across the top that contains the dark blue to light blue gradient.</li>
					<li>It contains the <strong>product name</strong>, which is always displayed in CAPS, with the first part offset by a bolder display.</li>
					<li>There is also an optional "User Identifier" menu control that can be turned on or off.</li>
				</ul>

				<h5>Main Navigation</h5>
				<ul>
					<li>This gray bar with the light gray to dark gray gradient also spans the width of the screen.</li>
					<li>It contains all of the main navigation sections (as defined by the specific project needs).</li>
					<li>Each navigation "tab" has a hover state on desktop version to provide user feedback.</li>
				</ul>
			</div>
			<div id="tabs-3">
				<p>Technical details go here&hellip;</p>
			</div>
			<div id="tabs-4">
				<p>This product header version is built responsively, and when the breakpoint is reached, the following will happen:</p>
				<ul>
					<li>The "User Identifier" control will drop off and will not display.</li>
					<li>Each navigation "tab" will stack on top of the other, on a separate line</li>
					<li>The product name text size will decrease.</li>
					<li>The "tab" text size will increase.</li>
				</ul>
			</div>
			<div id="tabs-5">
			<!-- GET CODE - BEGINS -->
				<section class="col-24">

					<h5 class="mul-hdr-rule">HTML</h5>
					<pre class="prettyprint linenums"><xmp>{% include '/base/default-header-profile-menu' %}</xmp></pre>

				</section>
			<!-- GET CODE - ENDS -->
			</div>
		</div>
	</div>
<!-- DETAILS SECTION - ENDS -->

</section>
{% endblock %}