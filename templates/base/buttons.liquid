{% block title %}{{ model.title }} : {{ global.site_title }}{% endblock %}
{% block content %}

<!-- INTRO SECTION - BEGINS -->
	<section class="col-24">
		<h1>{{ model.title }}</h1>
		<p class="mul-intro-text">
			There are two buttons styles: Primary and Secondary. For more detailed button color specifications check our the <a href="/base/colors#buttonColors">color palette</a>. 
		</p>
	</section>
<!-- INTRO SECTION - ENDS -->

<section class="col-24">

	<h3 class="mul-hdr-dbl-rule">
		Example
		<a href="javascript:;" class="mul-icon-size-small mul-icon-mul-arrow-circle-down mul-icon-color-gray-darker mul-icon-position-right mul-float-right related-items-menu"></a>
	</h3>

<!-- BUTTONS - BEGINS -->
	<div class="col-24 example-min-height">

		<div class="col-24">
			<div class="col-8 first">

				<div class="mul-box mul-box-rnd mul-padding-bottom mul-ref-example-box">
					<h4 class="mul-hdr-rule">Default Buttons</h4>

					<!-- BUTTONS - EXAMPLE - BEGINS -->
						<div class="col-24">
							<a class="mul-btn mul-btn-medium mul-btn-first" id="" href="" tabindex="" title="Secondary">Secondary</a>
							<button class="mul-btn mul-btn-medium mul-btn-primary" id="" name="" value="" type="" tabindex="" title="Primary">Primary</button>
						</div>
					<!-- BUTTONS - EXAMPLE - ENDS -->

				</div>
			</div>
			<div class="col-8">

				<div class="mul-box mul-box-rnd mul-padding-bottom mul-ref-example-box">
					<h4 class="mul-hdr-rule">Interactive Buttons</h4>

					<!-- BUTTONS - EXAMPLE - BEGINS -->
						<div class="col-24">
							{% include '/base/buttons/button-primary-interactive' %}
						</div>
					<!-- BUTTONS - EXAMPLE - ENDS -->

				</div>
			</div>

			<div class="col-8">
				<div class="mul-box mul-box-rnd mul-padding-bottom mul-ref-example-box">
					<h4 class="mul-hdr-rule">Disabled Buttons</h4>

					<!-- BUTTONS - EXAMPLE - BEGINS -->
						<div class="col-24">
							<button class="mul-btn mul-btn-medium mul-btn-first mul-btn-disabled" id="" name="" value="" type="" disabled="disabled" tabindex="" title="loading&hellip;">loading&hellip;</button>
							<button class="mul-btn mul-btn-medium mul-btn-primary mul-btn-disabled" id="" name="" value="" type="" disabled="disabled" tabindex="" title="loading&hellip;">loading&hellip;</button>
						</div>
					<!-- BUTTONS - EXAMPLE - ENDS -->

				</div>
			</div>
		</div>

		<div class="col-24">
			<div class="mul-box mul-box-rnd mul-padding-bottom mul-ref-example-box">
				<h4 class="mul-hdr-rule">Multi Step Example</h4>
				<p>In cases where buttons are used within a multi-step flow, the common combination of buttons is as follows: "Cancel" first, followed by "Back", followed by whatever the primary action is ("Next", "Save", "Review", etc).</p>
				
				<!-- BUTTONS - EXAMPLE - BEGINS -->
					<div class="col-24">
						<a class="mul-btn mul-btn-medium mul-btn-first" id="" href="" tabindex="" title="Cancel">Cancel</a>
						<a class="mul-btn mul-btn-medium" id="" href="" tabindex="" title="Back">Back</a>
						<button class="mul-btn mul-btn-medium mul-btn-primary" id="" name="" value="" type="" tabindex="" title="Save">Save</button>
					</div>
				<!-- BUTTONS - EXAMPLE - ENDS -->

			</div>
		</div>

		<div class="col-24">
			<div class="mul-box mul-box-rnd mul-padding-bottom mul-ref-example-box">
				<h4 class="mul-hdr-rule">Minimum Width</h4>
				<p>Each button has a minimum width of 100 pixels. This ensures consistency mul-even in cases where the label text is really short.</p>
				
				<!-- BUTTONS - EXAMPLE - BEGINS -->
					<div class="col-24">
						<a class="mul-btn mul-btn-medium mul-btn-first" id="" href="" tabindex="" title="Back">Back</a>
						<button class="mul-btn mul-btn-medium mul-btn-primary" id="" name="" value="" type="" tabindex="" title="Go">Go</button>
					</div>		
				<!-- BUTTONS - EXAMPLE - ENDS -->

			</div>
		</div>

		<div class="col-24">
			<div class="mul-box mul-box-rnd mul-padding-bottom mul-ref-example-box">
				<h4 class="mul-hdr-rule">Long Text</h4>
				<p>The buttons are content aware, and the width of the button should be dynamic based on the label text.</p>

				<!-- BUTTONS - EXAMPLE - BEGINS -->
					<div class="col-24">
						<button class="mul-btn mul-btn-medium mul-btn-primary mul-btn-first" id="" name="" value="" type="" tabindex="" title="Title Case Button With Alot Of Text">Title Case Button With Alot Of Text</button>
					</div>
				<!-- BUTTONS - EXAMPLE - ENDS -->

			</div>
		</div>

		<div class="col-24">
			<hr>
			<p>
				<a href="javascript:;" class="js-mul-toggle-content" data-mul-toggle="2">
					<i class="mul-icon-arrow-right3 mul-image-icon-position-left mul-image-icon-size-small"></i>Button Options
				</a>
			</p>
			<div class="mul-ref-example-box mul-display-none" id="div2">
				<h4 class="mul-hdr-rule">Button Sizes</h4>

				<!-- BUTTON SIZES TABLE - BEGINS -->
					<div class="mul-table-small-mediaquery">
						<table class="mul-table mul-table-borders" id="" border="0" cellpadding="0" cellspacing="0" title="" summary="">
							<thead>
								<tr>
									<th class="col-4" scope="col">Size</th>
									<th class="col-5" scope="col">Button Tag Primary</th>
									<th class="col-5" scope="col">Button Tag Secondary</th>
									<th class="col-5" scope="col">Link Tag Primary</th>
									<th class="col-5" scope="col">Link Tag Secondary</th>
								</tr>
							</thead>
							<tbody>

								{% for item in model.button_sizes_table_data %}
								<tr>
									<td class="mul-nowrap">
										{{ item.button_size_label }}
									</td>
									<td>
										<button class="mul-btn mul-btn-{{ item.button_size }} mul-btn-primary mul-btn-first" id="" name="" value="" type="" tabindex="" title="Button">Button</button>
									</td>
									<td>
										<button class="mul-btn mul-btn-{{ item.button_size }} mul-btn-first" id="" name="" value="" type="" tabindex="" title="Button">Button</button>
									</td>
									<td>
										<a class="mul-btn mul-btn-{{ item.button_size }} mul-btn-primary mul-btn-first" id="" href="" tabindex="" title="Button">Button</a>
									</td>
									<td>
										<a class="mul-btn mul-btn-{{ item.button_size }} mul-btn-first" id="" href="" tabindex="" title="Button">Button</a>
									</td>
								</tr>
								{% endfor %}

							</tbody>
						</table>
					</div>
				<!-- BUTTON SIZES TABLE - ENDS -->

			</div>
		</div>

	</div>
<!-- BUTTONS - ENDS -->

<!-- DETAILS SECTION - BEGINS -->
	<a name="moreDetails"></a>
	<h4 class="mul-hdr-dbl-rule additional-detail-hdr">Additional Details</h4>

	<div class="col-24">
		<div class="js-mul-tabs-example mul-tabs">
			{% include "page-tabs" %}
			<div id="tabs-1">
				<p>There are two button styles: Primary and Secondary.</p>
				<ul>
					<li> <strong>Primary Button</strong>
						- used for the primary action on the page. There is usually only one Primary Button on a screen in order to avoid user confusion. The Primary Button is essentially the "single most important" call-to-action you want the user to take. Multiple primary buttons will confuse the user and should be avoided.
					</li>
					<li> <strong>Secondary Button</strong>
						- Designed to be visually offset from the Primary button. There can be more than one Secondary Button on a screen. Secondary Buttons are essentially additional options the user has on the page, just not the "primary call-to-action."
					</li>
				</ul>
				<p>
					Buttons are different than links as links take you to another page, and therefore they have different uses. Buttons should be used for the following:
				</p>

				<ul>
					<li>
						As navigation in a multi-step flow (user will be going through a process or a wizard).
					</li>
					<li>To save data entered on a form</li>
					<li>
						As a click event action to invoke another interaction on the page. For example, toggling content, hiding and showing content, sliding a panel, etc.
					</li>
				</ul>
			</div>
			<div id="tabs-2">
				<ul>
					<li>
						Each button has 4 states: static, hover, active, and loading (optional).
					</li>
					<li>
						In the optional "loading" state, the button should be disabled to prevent the user from clicking multiple times. When this occurs, the button should also change style to also look like it's in a disabled state.
					</li>
					<li>
						Buttons are entirely created with CSS. No images should be used.
					</li>

					<li>
						Buttons used as "links" and buttons used with "forms" should look the same, but the functions can differ. For instance, you can make a link look like a button, but it will not have the "submit" function of a button with type of "submit."
					</li>
					<li>
						The buttons should be able to be aligned to one another horizontally for consistency of Mercer branding.
					</li>
				</ul>

				<h5>Width</h5>
				<ul>
					<li>
						Each button should have a <strong>minimum width of 100 pixels</strong>
						, but they can expand to match content length if needed.
					</li>
					<li>
						The final width of the button should be dynamic based on the content inside the button.
					</li>
				</ul>

				<h5>Labels</h5>
				<ul>
					<li>Button text should be a concise specific call to action.</li>
					<li>
						Button text should Title Case - except in the "loading" state where lowercase is used to visually offset this state from the others
					</li>
				</ul>

				<h5>Primary Buttons are:</h5>
				<ul>
					<li>Green gradient in default state</li>
					<li>Green gradient in active state</li>
					<li>Green gradient in hover state</li>
					<li>
						Green solid in loading state (text must also display "loading")
					</li>
				</ul>

				<h5>Secondary Buttons are:</h5>
				<ul>
					<li>Grey gradient in default state</li>
					<li>Grey gradient in active state</li>
					<li>Grey gradient in hover state</li>
					<li>
						Light grey solid in loading state (text must also display "loading")
					</li>
				</ul>
			</div>
			<div id="tabs-3">
				<h5>Cross Browser Compatibility</h5>
				<p>IE9 doesn't crop background gradients on rounded corners, so we square the corners of the button and it looks just like IE8. Related, with "Loading" buttons, IE8+9 jankifies disabled button elements, rendering text gray with a nasty text-shadow that we cannot fix.</p>

				<h4 class="mul-hdr-rule">Dependencies</h4>
				<table class="mul-table mul-table-borders" id="" border="0" cellpadding="0" cellspacing="0" title="" summary="">
					<thead>
						<tr>
							<th width="" scope="col">Library</th>
							<th width="" scope="col">Location or Download</th>
						</tr>
					</thead>
					<tbody>
						<tr>
							<td>jQuery {{ global.jquery_version }}</td>
							<td>assets/scripts/core/lib/jquery</td>
						</tr>
					</tbody>
				</table>
			</div>
			<div id="tabs-4">
				<p>{{ model.mobileIntroTabText }}</p>
			</div>
			<div id="tabs-5">
			<!-- GET CODE - BEGINS -->
				<section class="col-24">

					<h5 class="mul-hdr-rule">HTML</h5>
					<p>When using the buttons stacked side by side always use "mul-btn-first" class on the first button to remove the margin.</p>

					<h5>Primary Button</h5>
					<pre class="prettyprint linenums"><xmp>{% include '/base/buttons/button-primary' %}</xmp></pre>
					<pre class="prettyprint linenums"><xmp>{% include '/base/buttons/href-primary' %}</xmp></pre>

					<h5>Secondary Button</h5>
					<pre class="prettyprint linenums"><xmp>{% include '/base/buttons/button-secondary' %}</xmp></pre>
					<pre class="prettyprint linenums"><xmp>{% include '/base/buttons/href-secondary' %}</xmp></pre>

					<h5>Interactive Button</h5>
					<p>In order to create the interactivity that you will need the following class to be added: <code>js-mul-btn-loading</code>.</p>
					<pre class="prettyprint linenums"><xmp>{% include '/base/buttons/button-primary-interactive' %}</xmp></pre>

					<h5>Button With Size Class Helper ("Medium" as an example)</h5>
					<p>The "medium" size button is the default size of buttons. In order to get the button size of "medium", a class helper of <code>mul-btn-medium</code> is used. The following button size class helpers are also available:</p>
					<ol>
						<li><code>mul-btn-xxx-small</code></li>
						<li><code>mul-btn-xx-small</code></li>
						<li><code>mul-btn-x-small</code></li>
						<li><code>mul-btn-small</code></li>
						<li><code>mul-btn-medium</code></li>
						<li><code>mul-btn-large</code></li>
						<li><code>mul-btn-x-large</code></li>
						<li><code>mul-btn-xx-large</code></li>
						<li><code>mul-btn-xxx-large</code></li>
					</ol>
					<pre class="prettyprint linenums"><xmp>{% include '/base/buttons/button-primary' %}</xmp></pre>
					<pre class="prettyprint linenums"><xmp>{% include '/base/buttons/href-primary' %}</xmp></pre>

				</section>
			<!-- GET CODE - ENDS -->
			</div>
		</div>
	</div>
<!-- DETAILS SECTION - ENDS -->

</section>
{% endblock %}